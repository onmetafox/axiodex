{
  "name": "axiodex-prices",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "init:testnet": "graph init --studio prices-base-goerli",
    "auth:testnet": "graph auth --studio ad317bb9ff468283708f997e81ce837a",
    "deploy:testnet": "graph deploy --studio prices-base-goerli"
  }
}
