{
  "name": "axiodex-prices",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "init": "graph init --studio prices-base",
    "auth": "graph auth --studio 9210027020185cf1506687f9d4751095",
    "deploy:testnet": "graph deploy --studio prices-base-goerli",
    "deploy": "graph deploy --studio prices-base"
  }
}
