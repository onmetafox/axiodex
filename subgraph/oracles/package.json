{
  "name": "axiodex-prices",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "init": "graph init --studio oracle-base-goerli",
    "auth": "graph auth --studio bc72ce993df2ba1f8f624321d47ea650",
    "deploy": "graph deploy --studio oracle-base-goerli"
  }
}
